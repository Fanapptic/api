version: 0.2

phases:
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - npm test

artifacts:
  files:
    - appModules/**/*
    - config/**/*
    - libs/**/*
    - middlewares/**/*
    - models/**/*
    - routes/**/*
    - setup/**/*
    - .env
    - package.json
    - server.js
    - worker.js
